# include "main.h"

/**
 * _islower - Function to check for lowercase characters
 *@c: If c is lower return 1 otherwise return 0
 * Return: Always 0
 */
int _islower(int c)

{
if (c >= 97 && c <= 123)
{
	return (1);

}
else
	{
	return (0);
	}
}
